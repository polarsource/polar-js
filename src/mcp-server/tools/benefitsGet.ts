/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { benefitsGet } from "../../funcs/benefitsGet.js";
import { BenefitsGetRequest$inboundSchema } from "../../models/operations/benefitsget.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: BenefitsGetRequest$inboundSchema,
};

export const tool$benefitsGet: ToolDefinition<typeof args> = {
  name: "benefits_get",
  description: `Get Benefit

Get a benefit by ID.

**Scopes**: \`benefits:read\` \`benefits:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await benefitsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
