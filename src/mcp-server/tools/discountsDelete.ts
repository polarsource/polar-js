/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { discountsDelete } from "../../funcs/discountsDelete.js";
import { DiscountsDeleteRequest$inboundSchema } from "../../models/operations/discountsdelete.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DiscountsDeleteRequest$inboundSchema,
};

export const tool$discountsDelete: ToolDefinition<typeof args> = {
  name: "discounts_delete",
  description: `Delete Discount

Delete a discount.

**Scopes**: \`discounts:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await discountsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
