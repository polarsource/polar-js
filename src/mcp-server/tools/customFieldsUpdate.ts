/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customFieldsUpdate } from "../../funcs/customFieldsUpdate.js";
import { CustomFieldsUpdateRequest$inboundSchema } from "../../models/operations/customfieldsupdate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CustomFieldsUpdateRequest$inboundSchema,
};

export const tool$customFieldsUpdate: ToolDefinition<typeof args> = {
  name: "custom-fields_update",
  description: `Update Custom Field

Update a custom field.

**Scopes**: \`custom_fields:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customFieldsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
