/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauth2ClientsUpdate } from "../../funcs/oauth2ClientsUpdate.js";
import { Oauth2ClientsOauth2UpdateClientRequest$inboundSchema } from "../../models/operations/oauth2clientsoauth2updateclient.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: Oauth2ClientsOauth2UpdateClientRequest$inboundSchema,
};

export const tool$oauth2ClientsUpdate: ToolDefinition<typeof args> = {
  name: "oauth2-clients_update",
  description: `Update Client

Update an OAuth2 client.

**Scopes**:`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauth2ClientsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
