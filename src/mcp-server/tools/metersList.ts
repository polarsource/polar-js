/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metersList } from "../../funcs/metersList.js";
import { MetersListRequest$inboundSchema } from "../../models/operations/meterslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: MetersListRequest$inboundSchema,
};

export const tool$metersList: ToolDefinition<typeof args> = {
  name: "meters_list",
  description: `List Meters

List meters.

**Scopes**: \`meters:read\` \`meters:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
