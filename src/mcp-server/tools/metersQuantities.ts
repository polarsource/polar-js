/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metersQuantities } from "../../funcs/metersQuantities.js";
import { MetersQuantitiesRequest$inboundSchema } from "../../models/operations/metersquantities.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: MetersQuantitiesRequest$inboundSchema,
};

export const tool$metersQuantities: ToolDefinition<typeof args> = {
  name: "meters_quantities",
  description: `Get Meter Quantities

Get quantities of a meter over a time period.

**Scopes**: \`meters:read\` \`meters:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metersQuantities(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
