/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metersGet } from "../../funcs/metersGet.js";
import { MetersGetRequest$inboundSchema } from "../../models/operations/metersget.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: MetersGetRequest$inboundSchema,
};

export const tool$metersGet: ToolDefinition<typeof args> = {
  name: "meters_get",
  description: `Get Meter

Get a meter by ID.

**Scopes**: \`meters:read\` \`meters:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metersGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
