/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsIngest } from "../../funcs/eventsIngest.js";
import { EventsIngest$inboundSchema } from "../../models/components/eventsingest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EventsIngest$inboundSchema,
};

export const tool$eventsIngest: ToolDefinition<typeof args> = {
  name: "events_ingest",
  description: `Ingest Events

Ingest batch of events.

**Scopes**: \`events:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventsIngest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
