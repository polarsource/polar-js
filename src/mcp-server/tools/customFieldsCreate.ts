/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customFieldsCreate } from "../../funcs/customFieldsCreate.js";
import { CustomFieldCreate$inboundSchema } from "../../models/components/customfieldcreate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CustomFieldCreate$inboundSchema,
};

export const tool$customFieldsCreate: ToolDefinition<typeof args> = {
  name: "custom-fields_create",
  description: `Create Custom Field

Create a custom field.

**Scopes**: \`custom_fields:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customFieldsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
