/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metersEvents } from "../../funcs/metersEvents.js";
import { MetersEventsRequest$inboundSchema } from "../../models/operations/metersevents.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: MetersEventsRequest$inboundSchema,
};

export const tool$metersEvents: ToolDefinition<typeof args> = {
  name: "meters_events",
  description: `Get Meter Events

Get events matching the filter of a meter.

**Scopes**: \`meters:read\` \`meters:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metersEvents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
