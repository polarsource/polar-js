/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauth2ClientsCreate } from "../../funcs/oauth2ClientsCreate.js";
import { OAuth2ClientConfiguration$inboundSchema } from "../../models/components/oauth2clientconfiguration.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: OAuth2ClientConfiguration$inboundSchema,
};

export const tool$oauth2ClientsCreate: ToolDefinition<typeof args> = {
  name: "oauth2-clients_create",
  description: `Create Client

Create an OAuth2 client.

**Scopes**:`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauth2ClientsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
