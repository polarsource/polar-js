/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customerMetersGet } from "../../funcs/customerMetersGet.js";
import { CustomerMetersGetRequest$inboundSchema } from "../../models/operations/customermetersget.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CustomerMetersGetRequest$inboundSchema,
};

export const tool$customerMetersGet: ToolDefinition<typeof args> = {
  name: "customer-meters-get",
  description: `Get Customer Meter

Get a customer meter by ID.

**Scopes**: \`customer_meters:read\``,
  scopes: ["read", "customer_meters"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customerMetersGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
