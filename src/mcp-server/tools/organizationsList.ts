/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsList } from "../../funcs/organizationsList.js";
import { OrganizationsListRequest$inboundSchema } from "../../models/operations/organizationslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: OrganizationsListRequest$inboundSchema,
};

export const tool$organizationsList: ToolDefinition<typeof args> = {
  name: "organizations_list",
  description: `List Organizations

List organizations.

**Scopes**: \`organizations:read\` \`organizations:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
