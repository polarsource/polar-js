/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkoutsList } from "../../funcs/checkoutsList.js";
import { CheckoutsListRequest$inboundSchema } from "../../models/operations/checkoutslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CheckoutsListRequest$inboundSchema,
};

export const tool$checkoutsList: ToolDefinition<typeof args> = {
  name: "checkouts_list",
  description: `List Checkout Sessions

List checkout sessions.

**Scopes**: \`checkouts:read\` \`checkouts:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkoutsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
