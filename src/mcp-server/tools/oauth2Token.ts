/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauth2Token } from "../../funcs/oauth2Token.js";
import { Oauth2RequestTokenRequestBody$inboundSchema } from "../../models/operations/oauth2requesttoken.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: Oauth2RequestTokenRequestBody$inboundSchema,
};

export const tool$oauth2Token: ToolDefinition<typeof args> = {
  name: "oauth2_token",
  description: `Request Token

Request an access token using a valid grant.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauth2Token(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
