/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkoutLinksDelete } from "../../funcs/checkoutLinksDelete.js";
import { CheckoutLinksDeleteRequest$inboundSchema } from "../../models/operations/checkoutlinksdelete.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CheckoutLinksDeleteRequest$inboundSchema,
};

export const tool$checkoutLinksDelete: ToolDefinition<typeof args> = {
  name: "checkout-links_delete",
  description: `Delete Checkout Link

Delete a checkout link.

**Scopes**: \`checkout_links:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkoutLinksDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
