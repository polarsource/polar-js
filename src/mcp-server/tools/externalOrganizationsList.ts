/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { externalOrganizationsList } from "../../funcs/externalOrganizationsList.js";
import { ExternalOrganizationsListRequest$inboundSchema } from "../../models/operations/externalorganizationslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ExternalOrganizationsListRequest$inboundSchema,
};

export const tool$externalOrganizationsList: ToolDefinition<typeof args> = {
  name: "external-organizations_list",
  description: `List External Organizations

List external organizations.

**Scopes**: \`external_organizations:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await externalOrganizationsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
