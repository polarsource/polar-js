/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { benefitsGrants } from "../../funcs/benefitsGrants.js";
import { BenefitsGrantsRequest$inboundSchema } from "../../models/operations/benefitsgrants.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: BenefitsGrantsRequest$inboundSchema,
};

export const tool$benefitsGrants: ToolDefinition<typeof args> = {
  name: "benefits_grants",
  description: `List Benefit Grants

List the individual grants for a benefit.

It's especially useful to check if a user has been granted a benefit.

**Scopes**: \`benefits:read\` \`benefits:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await benefitsGrants(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
