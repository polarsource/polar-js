/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkoutsClientConfirm } from "../../funcs/checkoutsClientConfirm.js";
import { CheckoutsClientConfirmRequest$inboundSchema } from "../../models/operations/checkoutsclientconfirm.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CheckoutsClientConfirmRequest$inboundSchema,
};

export const tool$checkoutsClientConfirm: ToolDefinition<typeof args> = {
  name: "checkouts_client-confirm",
  description: `Confirm Checkout Session from Client

Confirm a checkout session by client secret.

Orders and subscriptions will be processed.

**Scopes**:`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkoutsClientConfirm(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
