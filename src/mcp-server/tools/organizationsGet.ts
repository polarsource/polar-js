/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsGet } from "../../funcs/organizationsGet.js";
import { OrganizationsGetRequest$inboundSchema } from "../../models/operations/organizationsget.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: OrganizationsGetRequest$inboundSchema,
};

export const tool$organizationsGet: ToolDefinition<typeof args> = {
  name: "organizations_get",
  description: `Get Organization

Get an organization by ID.

**Scopes**: \`organizations:read\` \`organizations:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
