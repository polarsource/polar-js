/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customFieldsList } from "../../funcs/customFieldsList.js";
import { CustomFieldsListRequest$inboundSchema } from "../../models/operations/customfieldslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CustomFieldsListRequest$inboundSchema,
};

export const tool$customFieldsList: ToolDefinition<typeof args> = {
  name: "custom-fields_list",
  description: `List Custom Fields

List custom fields.

**Scopes**: \`custom_fields:read\` \`custom_fields:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customFieldsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
