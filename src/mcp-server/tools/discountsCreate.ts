/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { discountsCreate } from "../../funcs/discountsCreate.js";
import { DiscountCreate$inboundSchema } from "../../models/components/discountcreate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DiscountCreate$inboundSchema,
};

export const tool$discountsCreate: ToolDefinition<typeof args> = {
  name: "discounts_create",
  description: `Create Discount

Create a discount.

**Scopes**: \`discounts:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await discountsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
