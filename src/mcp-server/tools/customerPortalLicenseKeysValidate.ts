/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customerPortalLicenseKeysValidate } from "../../funcs/customerPortalLicenseKeysValidate.js";
import { LicenseKeyValidate$inboundSchema } from "../../models/components/licensekeyvalidate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LicenseKeyValidate$inboundSchema,
};

export const tool$customerPortalLicenseKeysValidate: ToolDefinition<
  typeof args
> = {
  name: "customer-portal-license-keys_validate",
  description: `Validate License Key

Validate a license key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customerPortalLicenseKeysValidate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
