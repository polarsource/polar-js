/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customerPortalOrganizationsGet } from "../../funcs/customerPortalOrganizationsGet.js";
import { CustomerPortalOrganizationsGetRequest$inboundSchema } from "../../models/operations/customerportalorganizationsget.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CustomerPortalOrganizationsGetRequest$inboundSchema,
};

export const tool$customerPortalOrganizationsGet: ToolDefinition<typeof args> =
  {
    name: "customer-portal-organizations_get",
    description: `Get Organization

Get a customer portal's organization by slug.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await customerPortalOrganizationsGet(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
