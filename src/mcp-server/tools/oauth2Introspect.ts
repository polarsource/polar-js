/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauth2Introspect } from "../../funcs/oauth2Introspect.js";
import { IntrospectTokenRequest$inboundSchema } from "../../models/components/introspecttokenrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: IntrospectTokenRequest$inboundSchema,
};

export const tool$oauth2Introspect: ToolDefinition<typeof args> = {
  name: "oauth2_introspect",
  description: `Introspect Token

Get information about an access token.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauth2Introspect(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
