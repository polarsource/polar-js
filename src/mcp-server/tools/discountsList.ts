/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { discountsList } from "../../funcs/discountsList.js";
import { DiscountsListRequest$inboundSchema } from "../../models/operations/discountslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DiscountsListRequest$inboundSchema,
};

export const tool$discountsList: ToolDefinition<typeof args> = {
  name: "discounts_list",
  description: `List Discounts

List discounts.

**Scopes**: \`discounts:read\` \`discounts:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await discountsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
