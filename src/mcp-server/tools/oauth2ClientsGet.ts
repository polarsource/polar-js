/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauth2ClientsGet } from "../../funcs/oauth2ClientsGet.js";
import { Oauth2ClientsOauth2GetClientRequest$inboundSchema } from "../../models/operations/oauth2clientsoauth2getclient.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: Oauth2ClientsOauth2GetClientRequest$inboundSchema,
};

export const tool$oauth2ClientsGet: ToolDefinition<typeof args> = {
  name: "oauth2-clients_get",
  description: `Get Client

Get an OAuth2 client by Client ID.

**Scopes**:`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauth2ClientsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
