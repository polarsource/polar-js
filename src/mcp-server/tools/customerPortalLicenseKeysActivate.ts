/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customerPortalLicenseKeysActivate } from "../../funcs/customerPortalLicenseKeysActivate.js";
import { LicenseKeyActivate$inboundSchema } from "../../models/components/licensekeyactivate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LicenseKeyActivate$inboundSchema,
};

export const tool$customerPortalLicenseKeysActivate: ToolDefinition<
  typeof args
> = {
  name: "customer-portal-license-keys_activate",
  description: `Activate License Key

Activate a license key instance.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customerPortalLicenseKeysActivate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
