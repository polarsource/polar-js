/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customFieldsGet } from "../../funcs/customFieldsGet.js";
import { CustomFieldsGetRequest$inboundSchema } from "../../models/operations/customfieldsget.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CustomFieldsGetRequest$inboundSchema,
};

export const tool$customFieldsGet: ToolDefinition<typeof args> = {
  name: "custom-fields_get",
  description: `Get Custom Field

Get a custom field by ID.

**Scopes**: \`custom_fields:read\` \`custom_fields:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customFieldsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
