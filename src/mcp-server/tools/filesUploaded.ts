/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filesUploaded } from "../../funcs/filesUploaded.js";
import { FilesUploadedRequest$inboundSchema } from "../../models/operations/filesuploaded.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FilesUploadedRequest$inboundSchema,
};

export const tool$filesUploaded: ToolDefinition<typeof args> = {
  name: "files_uploaded",
  description: `Complete File Upload

Complete a file upload.

**Scopes**: \`files:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filesUploaded(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
