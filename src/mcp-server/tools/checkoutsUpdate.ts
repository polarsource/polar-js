/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkoutsUpdate } from "../../funcs/checkoutsUpdate.js";
import { CheckoutsUpdateRequest$inboundSchema } from "../../models/operations/checkoutsupdate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CheckoutsUpdateRequest$inboundSchema,
};

export const tool$checkoutsUpdate: ToolDefinition<typeof args> = {
  name: "checkouts_update",
  description: `Update Checkout Session

Update a checkout session.

**Scopes**: \`checkouts:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkoutsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
