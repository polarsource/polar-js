/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { licenseKeysGet } from "../../funcs/licenseKeysGet.js";
import { LicenseKeysGetRequest$inboundSchema } from "../../models/operations/licensekeysget.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LicenseKeysGetRequest$inboundSchema,
};

export const tool$licenseKeysGet: ToolDefinition<typeof args> = {
  name: "license-keys_get",
  description: `Get License Key

Get a license key.

**Scopes**: \`license_keys:read\` \`license_keys:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await licenseKeysGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
