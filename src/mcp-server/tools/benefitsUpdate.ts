/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { benefitsUpdate } from "../../funcs/benefitsUpdate.js";
import { BenefitsUpdateRequest$inboundSchema } from "../../models/operations/benefitsupdate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: BenefitsUpdateRequest$inboundSchema,
};

export const tool$benefitsUpdate: ToolDefinition<typeof args> = {
  name: "benefits_update",
  description: `Update Benefit

Update a benefit.

**Scopes**: \`benefits:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await benefitsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
