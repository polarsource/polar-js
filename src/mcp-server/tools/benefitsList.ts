/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { benefitsList } from "../../funcs/benefitsList.js";
import { BenefitsListRequest$inboundSchema } from "../../models/operations/benefitslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: BenefitsListRequest$inboundSchema,
};

export const tool$benefitsList: ToolDefinition<typeof args> = {
  name: "benefits_list",
  description: `List Benefits

List benefits.

**Scopes**: \`benefits:read\` \`benefits:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await benefitsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
