/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkoutsCreate } from "../../funcs/checkoutsCreate.js";
import { CheckoutCreate$inboundSchema } from "../../models/components/checkoutcreate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CheckoutCreate$inboundSchema,
};

export const tool$checkoutsCreate: ToolDefinition<typeof args> = {
  name: "checkouts_create",
  description: `Create Checkout Session

Create a checkout session.

**Scopes**: \`checkouts:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkoutsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
