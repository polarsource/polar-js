/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ordersGenerateInvoice } from "../../funcs/ordersGenerateInvoice.js";
import { OrdersGenerateInvoiceRequest$inboundSchema } from "../../models/operations/ordersgenerateinvoice.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: OrdersGenerateInvoiceRequest$inboundSchema,
};

export const tool$ordersGenerateInvoice: ToolDefinition<typeof args> = {
  name: "orders-generate-invoice",
  description: `Generate Order Invoice

Trigger generation of an order's invoice.

**Scopes**: \`orders:read\``,
  scopes: ["write", "orders"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ordersGenerateInvoice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
