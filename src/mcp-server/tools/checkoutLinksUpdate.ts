/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkoutLinksUpdate } from "../../funcs/checkoutLinksUpdate.js";
import { CheckoutLinksUpdateRequest$inboundSchema } from "../../models/operations/checkoutlinksupdate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CheckoutLinksUpdateRequest$inboundSchema,
};

export const tool$checkoutLinksUpdate: ToolDefinition<typeof args> = {
  name: "checkout-links_update",
  description: `Update Checkout Link

Update a checkout link.

**Scopes**: \`checkout_links:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkoutLinksUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
