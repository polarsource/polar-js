/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauth2ClientsList } from "../../funcs/oauth2ClientsList.js";
import { Oauth2ClientsListRequest$inboundSchema } from "../../models/operations/oauth2clientslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: Oauth2ClientsListRequest$inboundSchema,
};

export const tool$oauth2ClientsList: ToolDefinition<typeof args> = {
  name: "oauth2-clients_list",
  description: `List Clients

List OAuth2 clients.

**Scopes**:`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauth2ClientsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
