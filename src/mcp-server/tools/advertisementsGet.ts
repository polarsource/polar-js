/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { advertisementsGet } from "../../funcs/advertisementsGet.js";
import { AdvertisementsGetRequest$inboundSchema } from "../../models/operations/advertisementsget.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AdvertisementsGetRequest$inboundSchema,
};

export const tool$advertisementsGet: ToolDefinition<typeof args> = {
  name: "advertisements_get",
  description: `Get Campaign

Get an advertisement campaign by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await advertisementsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
