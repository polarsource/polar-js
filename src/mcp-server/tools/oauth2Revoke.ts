/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauth2Revoke } from "../../funcs/oauth2Revoke.js";
import { RevokeTokenRequest$inboundSchema } from "../../models/components/revoketokenrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RevokeTokenRequest$inboundSchema,
};

export const tool$oauth2Revoke: ToolDefinition<typeof args> = {
  name: "oauth2_revoke",
  description: `Revoke Token

Revoke an access token or a refresh token.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauth2Revoke(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
