/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customerMetersList } from "../../funcs/customerMetersList.js";
import { CustomerMetersListRequest$inboundSchema } from "../../models/operations/customermeterslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CustomerMetersListRequest$inboundSchema,
};

export const tool$customerMetersList: ToolDefinition<typeof args> = {
  name: "customer-meters-list",
  description: `List Customer Meters

List customer meters.

**Scopes**: \`customer_meters:read\``,
  scopes: ["read", "customer_meters"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customerMetersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
