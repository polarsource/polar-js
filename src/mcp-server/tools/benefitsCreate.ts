/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { benefitsCreate } from "../../funcs/benefitsCreate.js";
import { BenefitCreate$inboundSchema } from "../../models/components/benefitcreate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: BenefitCreate$inboundSchema,
};

export const tool$benefitsCreate: ToolDefinition<typeof args> = {
  name: "benefits_create",
  description: `Create Benefit

Create a benefit.

**Scopes**: \`benefits:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await benefitsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
