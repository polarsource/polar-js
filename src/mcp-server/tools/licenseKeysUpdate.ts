/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { licenseKeysUpdate } from "../../funcs/licenseKeysUpdate.js";
import { LicenseKeysUpdateRequest$inboundSchema } from "../../models/operations/licensekeysupdate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LicenseKeysUpdateRequest$inboundSchema,
};

export const tool$licenseKeysUpdate: ToolDefinition<typeof args> = {
  name: "license-keys_update",
  description: `Update License Key

Update a license key.

**Scopes**: \`license_keys:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await licenseKeysUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
