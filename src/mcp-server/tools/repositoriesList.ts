/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repositoriesList } from "../../funcs/repositoriesList.js";
import { RepositoriesListRequest$inboundSchema } from "../../models/operations/repositorieslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RepositoriesListRequest$inboundSchema,
};

export const tool$repositoriesList: ToolDefinition<typeof args> = {
  name: "repositories_list",
  description: `List Repositories

List repositories.

**Scopes**: \`repositories:read\` \`repositories:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repositoriesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
