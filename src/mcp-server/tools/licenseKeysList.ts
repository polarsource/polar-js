/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { licenseKeysList } from "../../funcs/licenseKeysList.js";
import { LicenseKeysListRequest$inboundSchema } from "../../models/operations/licensekeyslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LicenseKeysListRequest$inboundSchema,
};

export const tool$licenseKeysList: ToolDefinition<typeof args> = {
  name: "license-keys_list",
  description: `List License Keys

Get license keys connected to the given organization & filters.

**Scopes**: \`license_keys:read\` \`license_keys:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await licenseKeysList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
