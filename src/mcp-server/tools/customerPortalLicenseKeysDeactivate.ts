/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customerPortalLicenseKeysDeactivate } from "../../funcs/customerPortalLicenseKeysDeactivate.js";
import { LicenseKeyDeactivate$inboundSchema } from "../../models/components/licensekeydeactivate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LicenseKeyDeactivate$inboundSchema,
};

export const tool$customerPortalLicenseKeysDeactivate: ToolDefinition<
  typeof args
> = {
  name: "customer-portal-license-keys_deactivate",
  description: `Deactivate License Key

Deactivate a license key instance.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customerPortalLicenseKeysDeactivate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
