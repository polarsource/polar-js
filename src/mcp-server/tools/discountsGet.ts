/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { discountsGet } from "../../funcs/discountsGet.js";
import { DiscountsGetRequest$inboundSchema } from "../../models/operations/discountsget.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DiscountsGetRequest$inboundSchema,
};

export const tool$discountsGet: ToolDefinition<typeof args> = {
  name: "discounts_get",
  description: `Get Discount

Get a discount by ID.

**Scopes**: \`discounts:read\` \`discounts:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await discountsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
