/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { advertisementsList } from "../../funcs/advertisementsList.js";
import { AdvertisementsListRequest$inboundSchema } from "../../models/operations/advertisementslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AdvertisementsListRequest$inboundSchema,
};

export const tool$advertisementsList: ToolDefinition<typeof args> = {
  name: "advertisements_list",
  description: `List Campaigns

List active advertisement campaigns for a benefit.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await advertisementsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
