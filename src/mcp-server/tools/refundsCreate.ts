/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { refundsCreate } from "../../funcs/refundsCreate.js";
import { RefundCreate$inboundSchema } from "../../models/components/refundcreate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RefundCreate$inboundSchema,
};

export const tool$refundsCreate: ToolDefinition<typeof args> = {
  name: "refunds_create",
  description: `Create Refund

Create a refund.

**Scopes**: \`refunds:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await refundsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
