/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filesCreate } from "../../funcs/filesCreate.js";
import { FileCreate$inboundSchema } from "../../models/components/filecreate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileCreate$inboundSchema,
};

export const tool$filesCreate: ToolDefinition<typeof args> = {
  name: "files_create",
  description: `Create File

Create a file.

**Scopes**: \`files:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
