/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { refundsList } from "../../funcs/refundsList.js";
import { RefundsListRequest$inboundSchema } from "../../models/operations/refundslist.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RefundsListRequest$inboundSchema,
};

export const tool$refundsList: ToolDefinition<typeof args> = {
  name: "refunds_list",
  description: `List Refunds

List products.

**Scopes**: \`refunds:read\` \`refunds:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await refundsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
