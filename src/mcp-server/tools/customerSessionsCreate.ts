/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customerSessionsCreate } from "../../funcs/customerSessionsCreate.js";
import { CustomerSessionCreate$inboundSchema } from "../../models/components/customersessioncreate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CustomerSessionCreate$inboundSchema,
};

export const tool$customerSessionsCreate: ToolDefinition<typeof args> = {
  name: "customer-sessions_create",
  description: `Create Customer Session

Create a customer session.

**Scopes**: \`customer_sessions:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customerSessionsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
