/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repositoriesUpdate } from "../../funcs/repositoriesUpdate.js";
import { RepositoriesUpdateRequest$inboundSchema } from "../../models/operations/repositoriesupdate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RepositoriesUpdateRequest$inboundSchema,
};

export const tool$repositoriesUpdate: ToolDefinition<typeof args> = {
  name: "repositories_update",
  description: `Update Repository

Update a repository.

**Scopes**: \`repositories:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repositoriesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
