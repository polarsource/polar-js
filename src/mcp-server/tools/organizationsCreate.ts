/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsCreate } from "../../funcs/organizationsCreate.js";
import { OrganizationCreate$inboundSchema } from "../../models/components/organizationcreate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: OrganizationCreate$inboundSchema,
};

export const tool$organizationsCreate: ToolDefinition<typeof args> = {
  name: "organizations_create",
  description: `Create Organization

Create an organization.

**Scopes**: \`organizations:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
