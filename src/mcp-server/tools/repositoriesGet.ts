/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repositoriesGet } from "../../funcs/repositoriesGet.js";
import { RepositoriesGetRequest$inboundSchema } from "../../models/operations/repositoriesget.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RepositoriesGetRequest$inboundSchema,
};

export const tool$repositoriesGet: ToolDefinition<typeof args> = {
  name: "repositories_get",
  description: `Get Repository

Get a repository by ID.

**Scopes**: \`repositories:read\` \`repositories:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repositoriesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
