/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metersUpdate } from "../../funcs/metersUpdate.js";
import { MetersUpdateRequest$inboundSchema } from "../../models/operations/metersupdate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: MetersUpdateRequest$inboundSchema,
};

export const tool$metersUpdate: ToolDefinition<typeof args> = {
  name: "meters_update",
  description: `Update Meter

Update a meter.

**Scopes**: \`meters:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metersUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
