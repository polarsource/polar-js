/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { licenseKeysGetActivation } from "../../funcs/licenseKeysGetActivation.js";
import { LicenseKeysGetActivationRequest$inboundSchema } from "../../models/operations/licensekeysgetactivation.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LicenseKeysGetActivationRequest$inboundSchema,
};

export const tool$licenseKeysGetActivation: ToolDefinition<typeof args> = {
  name: "license-keys_get-activation",
  description: `Get Activation

Get a license key activation.

**Scopes**: \`license_keys:read\` \`license_keys:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await licenseKeysGetActivation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
