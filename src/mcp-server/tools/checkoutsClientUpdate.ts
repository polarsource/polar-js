/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkoutsClientUpdate } from "../../funcs/checkoutsClientUpdate.js";
import { CheckoutsClientUpdateRequest$inboundSchema } from "../../models/operations/checkoutsclientupdate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CheckoutsClientUpdateRequest$inboundSchema,
};

export const tool$checkoutsClientUpdate: ToolDefinition<typeof args> = {
  name: "checkouts_client-update",
  description: `Update Checkout Session from Client

Update a checkout session by client secret.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkoutsClientUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
