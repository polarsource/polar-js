/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  BenefitFeatureFlagProperties,
  BenefitFeatureFlagProperties$inboundSchema,
  BenefitFeatureFlagProperties$Outbound,
  BenefitFeatureFlagProperties$outboundSchema,
} from "./benefitfeatureflagproperties.js";
import {
  MetadataOutputType,
  MetadataOutputType$inboundSchema,
  MetadataOutputType$Outbound,
  MetadataOutputType$outboundSchema,
} from "./metadataoutputtype.js";

/**
 * A benefit of type `feature_flag`.
 *
 * @remarks
 *
 * Use it to grant feature flags with key-value metadata
 * that can be queried via the API and webhooks.
 */
export type BenefitFeatureFlag = {
  /**
   * The ID of the benefit.
   */
  id: string;
  /**
   * Creation timestamp of the object.
   */
  createdAt: Date;
  /**
   * Last modification timestamp of the object.
   */
  modifiedAt: Date | null;
  type: "feature_flag";
  /**
   * The description of the benefit.
   */
  description: string;
  /**
   * Whether the benefit is selectable when creating a product.
   */
  selectable: boolean;
  /**
   * Whether the benefit is deletable.
   */
  deletable: boolean;
  /**
   * The ID of the organization owning the benefit.
   */
  organizationId: string;
  metadata: { [k: string]: MetadataOutputType };
  /**
   * Properties for a benefit of type `feature_flag`.
   */
  properties: BenefitFeatureFlagProperties;
};

/** @internal */
export const BenefitFeatureFlag$inboundSchema: z.ZodMiniType<
  BenefitFeatureFlag,
  unknown
> = z.pipe(
  z.object({
    id: z.string(),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    modified_at: z.nullable(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    type: z.literal("feature_flag"),
    description: z.string(),
    selectable: z.boolean(),
    deletable: z.boolean(),
    organization_id: z.string(),
    metadata: z.record(z.string(), MetadataOutputType$inboundSchema),
    properties: BenefitFeatureFlagProperties$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "modified_at": "modifiedAt",
      "organization_id": "organizationId",
    });
  }),
);
/** @internal */
export type BenefitFeatureFlag$Outbound = {
  id: string;
  created_at: string;
  modified_at: string | null;
  type: "feature_flag";
  description: string;
  selectable: boolean;
  deletable: boolean;
  organization_id: string;
  metadata: { [k: string]: MetadataOutputType$Outbound };
  properties: BenefitFeatureFlagProperties$Outbound;
};

/** @internal */
export const BenefitFeatureFlag$outboundSchema: z.ZodMiniType<
  BenefitFeatureFlag$Outbound,
  BenefitFeatureFlag
> = z.pipe(
  z.object({
    id: z.string(),
    createdAt: z.pipe(z.date(), z.transform(v => v.toISOString())),
    modifiedAt: z.nullable(z.pipe(z.date(), z.transform(v => v.toISOString()))),
    type: z.literal("feature_flag"),
    description: z.string(),
    selectable: z.boolean(),
    deletable: z.boolean(),
    organizationId: z.string(),
    metadata: z.record(z.string(), MetadataOutputType$outboundSchema),
    properties: BenefitFeatureFlagProperties$outboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      createdAt: "created_at",
      modifiedAt: "modified_at",
      organizationId: "organization_id",
    });
  }),
);

export function benefitFeatureFlagToJSON(
  benefitFeatureFlag: BenefitFeatureFlag,
): string {
  return JSON.stringify(
    BenefitFeatureFlag$outboundSchema.parse(benefitFeatureFlag),
  );
}
export function benefitFeatureFlagFromJSON(
  jsonString: string,
): SafeParseResult<BenefitFeatureFlag, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BenefitFeatureFlag$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BenefitFeatureFlag' from JSON`,
  );
}
