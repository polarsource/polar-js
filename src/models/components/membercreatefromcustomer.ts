/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { ClosedEnum } from "../../types/enums.js";

/**
 * The role of the member within the customer. To assign or transfer ownership, use the member update endpoint.
 */
export const Role = {
  Member: "member",
  BillingManager: "billing_manager",
} as const;
/**
 * The role of the member within the customer. To assign or transfer ownership, use the member update endpoint.
 */
export type Role = ClosedEnum<typeof Role>;

/**
 * Schema for creating a new member nested under a customer.
 *
 * @remarks
 *
 * The customer is taken from the URL path, so it's not part of the body.
 */
export type MemberCreateFromCustomer = {
  /**
   * The email address of the member.
   */
  email: string;
  name?: string | null | undefined;
  /**
   * The ID of the member in your system. This must be unique within the customer.
   */
  externalId?: string | null | undefined;
  /**
   * The role of the member within the customer. To assign or transfer ownership, use the member update endpoint.
   */
  role?: Role | undefined;
};

/** @internal */
export const Role$outboundSchema: z.ZodMiniEnum<typeof Role> = z.enum(Role);

/** @internal */
export type MemberCreateFromCustomer$Outbound = {
  email: string;
  name?: string | null | undefined;
  external_id?: string | null | undefined;
  role: string;
};

/** @internal */
export const MemberCreateFromCustomer$outboundSchema: z.ZodMiniType<
  MemberCreateFromCustomer$Outbound,
  MemberCreateFromCustomer
> = z.pipe(
  z.object({
    email: z.string(),
    name: z.optional(z.nullable(z.string())),
    externalId: z.optional(z.nullable(z.string())),
    role: z._default(Role$outboundSchema, "member"),
  }),
  z.transform((v) => {
    return remap$(v, {
      externalId: "external_id",
    });
  }),
);

export function memberCreateFromCustomerToJSON(
  memberCreateFromCustomer: MemberCreateFromCustomer,
): string {
  return JSON.stringify(
    MemberCreateFromCustomer$outboundSchema.parse(memberCreateFromCustomer),
  );
}
