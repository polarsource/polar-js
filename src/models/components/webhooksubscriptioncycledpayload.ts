/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  Subscription,
  Subscription$inboundSchema,
  Subscription$Outbound,
  Subscription$outboundSchema,
} from "./subscription.js";

/**
 * Sent when a subscription enters a new billing period.
 *
 * @remarks
 *
 * The payload carries the new `current_period_start` and `current_period_end`.
 * It fires when the period rolls over, before the renewal order exists and
 * regardless of whether the renewal payment succeeds — listen to `order.paid`
 * if you need the payment.
 *
 * A trial converting to a paid subscription starts a new period, so it fires
 * there too. Read `status` to tell the two apart.
 *
 * **Discord & Slack support:** Basic
 */
export type WebhookSubscriptionCycledPayload = {
  type: "subscription.cycled";
  timestamp: Date;
  data: Subscription;
};

/** @internal */
export const WebhookSubscriptionCycledPayload$inboundSchema: z.ZodMiniType<
  WebhookSubscriptionCycledPayload,
  unknown
> = z.object({
  type: z.literal("subscription.cycled"),
  timestamp: z.pipe(
    z.iso.datetime({ offset: true }),
    z.transform(v => new Date(v)),
  ),
  data: Subscription$inboundSchema,
});
/** @internal */
export type WebhookSubscriptionCycledPayload$Outbound = {
  type: "subscription.cycled";
  timestamp: string;
  data: Subscription$Outbound;
};

/** @internal */
export const WebhookSubscriptionCycledPayload$outboundSchema: z.ZodMiniType<
  WebhookSubscriptionCycledPayload$Outbound,
  WebhookSubscriptionCycledPayload
> = z.object({
  type: z.literal("subscription.cycled"),
  timestamp: z.pipe(z.date(), z.transform(v => v.toISOString())),
  data: Subscription$outboundSchema,
});

export function webhookSubscriptionCycledPayloadToJSON(
  webhookSubscriptionCycledPayload: WebhookSubscriptionCycledPayload,
): string {
  return JSON.stringify(
    WebhookSubscriptionCycledPayload$outboundSchema.parse(
      webhookSubscriptionCycledPayload,
    ),
  );
}
export function webhookSubscriptionCycledPayloadFromJSON(
  jsonString: string,
): SafeParseResult<WebhookSubscriptionCycledPayload, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WebhookSubscriptionCycledPayload$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WebhookSubscriptionCycledPayload' from JSON`,
  );
}
