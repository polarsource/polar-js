/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

/**
 * Schema for creating a member session using a member ID.
 */
export type MemberSessionCreate = {
  /**
   * ID of the member to create a session for.
   */
  memberId: string;
  /**
   * When set, a back button will be shown in the customer portal to return to this URL.
   */
  returnUrl?: string | null | undefined;
};

/** @internal */
export type MemberSessionCreate$Outbound = {
  member_id: string;
  return_url?: string | null | undefined;
};

/** @internal */
export const MemberSessionCreate$outboundSchema: z.ZodMiniType<
  MemberSessionCreate$Outbound,
  MemberSessionCreate
> = z.pipe(
  z.object({
    memberId: z.string(),
    returnUrl: z.optional(z.nullable(z.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      memberId: "member_id",
      returnUrl: "return_url",
    });
  }),
);

export function memberSessionCreateToJSON(
  memberSessionCreate: MemberSessionCreate,
): string {
  return JSON.stringify(
    MemberSessionCreate$outboundSchema.parse(memberSessionCreate),
  );
}
