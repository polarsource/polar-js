/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  Checkout,
  Checkout$inboundSchema,
  Checkout$Outbound,
  Checkout$outboundSchema,
} from "./checkout.js";

/**
 * Sent when a checkout expires.
 *
 * @remarks
 *
 * This event fires when a checkout reaches its expiration time without being completed.
 * Developers can use this to send reminder emails or track checkout abandonment.
 *
 * **Discord & Slack support:** Basic
 */
export type WebhookCheckoutExpiredPayload = {
  type: "checkout.expired";
  timestamp: Date;
  /**
   * Checkout session data retrieved using an access token.
   */
  data: Checkout;
};

/** @internal */
export const WebhookCheckoutExpiredPayload$inboundSchema: z.ZodMiniType<
  WebhookCheckoutExpiredPayload,
  unknown
> = z.object({
  type: z.literal("checkout.expired"),
  timestamp: z.pipe(
    z.iso.datetime({ offset: true }),
    z.transform(v => new Date(v)),
  ),
  data: Checkout$inboundSchema,
});
/** @internal */
export type WebhookCheckoutExpiredPayload$Outbound = {
  type: "checkout.expired";
  timestamp: string;
  data: Checkout$Outbound;
};

/** @internal */
export const WebhookCheckoutExpiredPayload$outboundSchema: z.ZodMiniType<
  WebhookCheckoutExpiredPayload$Outbound,
  WebhookCheckoutExpiredPayload
> = z.object({
  type: z.literal("checkout.expired"),
  timestamp: z.pipe(z.date(), z.transform(v => v.toISOString())),
  data: Checkout$outboundSchema,
});

export function webhookCheckoutExpiredPayloadToJSON(
  webhookCheckoutExpiredPayload: WebhookCheckoutExpiredPayload,
): string {
  return JSON.stringify(
    WebhookCheckoutExpiredPayload$outboundSchema.parse(
      webhookCheckoutExpiredPayload,
    ),
  );
}
export function webhookCheckoutExpiredPayloadFromJSON(
  jsonString: string,
): SafeParseResult<WebhookCheckoutExpiredPayload, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WebhookCheckoutExpiredPayload$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WebhookCheckoutExpiredPayload' from JSON`,
  );
}
