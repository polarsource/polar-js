/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type OrderUnvoidedMetadata = {
  orderId: string;
  amount: number;
  currency: string;
};

/** @internal */
export const OrderUnvoidedMetadata$inboundSchema: z.ZodMiniType<
  OrderUnvoidedMetadata,
  unknown
> = z.pipe(
  z.object({
    order_id: z.string(),
    amount: z.int(),
    currency: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "order_id": "orderId",
    });
  }),
);

export function orderUnvoidedMetadataFromJSON(
  jsonString: string,
): SafeParseResult<OrderUnvoidedMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OrderUnvoidedMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OrderUnvoidedMetadata' from JSON`,
  );
}
