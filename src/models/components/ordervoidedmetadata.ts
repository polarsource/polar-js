/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type OrderVoidedMetadata = {
  orderId: string;
  amount: number;
  currency: string;
};

/** @internal */
export const OrderVoidedMetadata$inboundSchema: z.ZodMiniType<
  OrderVoidedMetadata,
  unknown
> = z.pipe(
  z.object({
    order_id: z.string(),
    amount: z.int(),
    currency: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "order_id": "orderId",
    });
  }),
);

export function orderVoidedMetadataFromJSON(
  jsonString: string,
): SafeParseResult<OrderVoidedMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OrderVoidedMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OrderVoidedMetadata' from JSON`,
  );
}
