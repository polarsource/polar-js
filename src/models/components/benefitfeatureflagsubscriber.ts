/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  BenefitFeatureFlagSubscriberProperties,
  BenefitFeatureFlagSubscriberProperties$inboundSchema,
} from "./benefitfeatureflagsubscriberproperties.js";
import {
  BenefitSubscriberOrganization,
  BenefitSubscriberOrganization$inboundSchema,
} from "./benefitsubscriberorganization.js";
import {
  MetadataOutputType,
  MetadataOutputType$inboundSchema,
} from "./metadataoutputtype.js";

export type BenefitFeatureFlagSubscriber = {
  /**
   * The ID of the benefit.
   */
  id: string;
  /**
   * Creation timestamp of the object.
   */
  createdAt: Date;
  /**
   * Last modification timestamp of the object.
   */
  modifiedAt: Date | null;
  type: "feature_flag";
  /**
   * The description of the benefit.
   */
  description: string;
  /**
   * Whether the benefit is selectable when creating a product.
   */
  selectable: boolean;
  /**
   * Whether the benefit is deletable.
   */
  deletable: boolean;
  /**
   * The ID of the organization owning the benefit.
   */
  organizationId: string;
  metadata: { [k: string]: MetadataOutputType };
  organization: BenefitSubscriberOrganization;
  /**
   * Properties available to subscribers for a benefit of type `feature_flag`.
   */
  properties: BenefitFeatureFlagSubscriberProperties;
};

/** @internal */
export const BenefitFeatureFlagSubscriber$inboundSchema: z.ZodMiniType<
  BenefitFeatureFlagSubscriber,
  unknown
> = z.pipe(
  z.object({
    id: z.string(),
    created_at: z.pipe(
      z.iso.datetime({ offset: true }),
      z.transform(v => new Date(v)),
    ),
    modified_at: z.nullable(
      z.pipe(z.iso.datetime({ offset: true }), z.transform(v => new Date(v))),
    ),
    type: z.literal("feature_flag"),
    description: z.string(),
    selectable: z.boolean(),
    deletable: z.boolean(),
    organization_id: z.string(),
    metadata: z.record(z.string(), MetadataOutputType$inboundSchema),
    organization: BenefitSubscriberOrganization$inboundSchema,
    properties: BenefitFeatureFlagSubscriberProperties$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "modified_at": "modifiedAt",
      "organization_id": "organizationId",
    });
  }),
);

export function benefitFeatureFlagSubscriberFromJSON(
  jsonString: string,
): SafeParseResult<BenefitFeatureFlagSubscriber, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BenefitFeatureFlagSubscriber$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BenefitFeatureFlagSubscriber' from JSON`,
  );
}
